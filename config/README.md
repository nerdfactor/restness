# RESTness Config

This module defines configuration elements used by the RESTness library. The configuration
is the intermediate state that describes the controllers that will be created. The configuration
can be created from files or from annotation processing.